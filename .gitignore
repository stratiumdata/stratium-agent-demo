# OS / editor
.DS_Store

# Secrets — these belong only in your local environment.
# Use .env.example as a template; copy to .env and fill in real values.
.env
.env.local
.env.*.local

# Node bits (the agent containers install node_modules, but those don't
# belong in the repo since they're regenerated at image build time).
node_modules/
mcp/node_modules/
mcp/package-lock.json
